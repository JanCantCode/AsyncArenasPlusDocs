{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"AsyncArenasPlus","text":"<p>AsyncArenasPlus helps you create and manage resettable arena areas.</p> <p>This documentation is written for server owners and operators, not Java developers.</p>"},{"location":"#what-you-can-do","title":"What you can do","text":"<ul> <li>Create arenas from a wand selection or exact coordinates.</li> <li>Reset Arenas / Areas of your server with zero lag.</li> <li>Rotate one gameplay area between multiple saved terrain variants.</li> <li>Show reset countdowns with PlaceholderAPI.</li> <li>Migrate arenas from supported reset plugins.</li> </ul>"},{"location":"#optional-integrations","title":"Optional integrations","text":"<ul> <li>PlaceholderAPI: enables <code>%asyncarenasplus_...%</code> placeholders.</li> <li>AsyncArenas, AreaResetterPro, or AreaReloader-FAWE: enables migration from those plugins.</li> </ul>"},{"location":"#quick-start","title":"Quick start","text":"<p>Go to Getting Started if this is your first setup.</p> <p>For a full command list, see Arena Commands.</p> <p>If you are comparing Minecraft region reset plugins, read AsyncArenasPlus vs WorldEdit, FAWE, and arena reset tools.</p>"},{"location":"commands/","title":"Arena Commands","text":"<p>All normal arena commands are under:</p> <pre><code>/arena\n</code></pre> <p>Required permission:</p> <pre><code>asyncarenasplus.admin\n</code></pre>"},{"location":"commands/#time-values","title":"Time values","text":"<p>Commands that accept time use the same parser:</p> <ul> <li><code>10s</code>: seconds</li> <li><code>5m</code>: minutes</li> <li><code>3h</code>: hours</li> <li><code>1d</code>: days</li> <li><code>20t</code>: ticks</li> </ul> <p>Use an explicit unit in commands. No-suffix values are treated as ticks.</p>"},{"location":"commands/#create","title":"Create","text":"<p>Create from your current wand selection:</p> <pre><code>/arena create &lt;name&gt; [time]\n</code></pre> <p>Create from exact coordinates:</p> <pre><code>/arena create &lt;name&gt; &lt;world&gt; &lt;x1&gt; &lt;y1&gt; &lt;z1&gt; &lt;x2&gt; &lt;y2&gt; &lt;z2&gt; [time]\n</code></pre> <p>If a time is provided, it becomes the arena's periodic reset interval.</p>"},{"location":"commands/#wand","title":"Wand","text":"<pre><code>/arena wand\n</code></pre> <p>Gives the arena selection amethyst shard to an in-game player.</p> <p>Selection controls:</p> <ul> <li>Left click: position 1</li> <li>Right click: position 2</li> </ul>"},{"location":"commands/#reset","title":"Reset","text":"<pre><code>/arena reset &lt;name&gt;\n</code></pre> <p>Runs a manual arena reset.</p>"},{"location":"commands/#edit","title":"Edit","text":"<pre><code>/arena edit &lt;name&gt;\n</code></pre> <p>Saves the arena from the current world state while keeping the same bounds and schedule.</p> <p>Recommended workflow:</p> <pre><code>/arena pause &lt;time&gt;\n/arena reset &lt;name&gt;\n</code></pre> <p>Make your changes, then run:</p> <pre><code>/arena edit &lt;name&gt;\n/arena pause off\n</code></pre>"},{"location":"commands/#info","title":"Info","text":"<pre><code>/arena info &lt;name&gt;\n</code></pre> <p>Shows:</p> <ul> <li>display name</li> <li>world</li> <li>bounds</li> <li>clickable midpoint teleport</li> <li>automatic reset schedule and next reset time</li> </ul>"},{"location":"commands/#list","title":"List","text":"<pre><code>/arena list\n</code></pre> <p>Shows all loaded arenas with display name, world, and periodic reset schedule.</p>"},{"location":"commands/#display-name","title":"Display Name","text":"<pre><code>/arena displayname &lt;arena&gt; &lt;new display name&gt;\n</code></pre> <p>Sets the public display name stored in <code>config.yml</code> under <code>arena-display-names</code>.</p> <p>Arena files use the name you entered when creating the arena. Display names let placeholders and command output show a cleaner public name without renaming the arena file.</p> <p>Example:</p> <pre><code>/arena displayname pvp_1 PVP Arena 1\n</code></pre>"},{"location":"commands/#periodic-reset-schedule","title":"Periodic Reset Schedule","text":"<pre><code>/arena periodic &lt;name&gt; &lt;time&gt; [offset]\n</code></pre> <p>Behavior:</p> <ul> <li><code>time</code> sets the interval.</li> <li><code>offset</code> is optional and delays when that arena starts its schedule.</li> <li>You do not need to specify an offset.</li> <li>A <code>time</code> of <code>0</code> disables automatic resets for that arena.</li> </ul> <p>Examples:</p> <pre><code>/arena periodic mine_1 15m\n/arena periodic mine_1 15m 2m\n/arena periodic mine_1 0\n</code></pre>"},{"location":"commands/#rotation-setup","title":"Rotation Setup","text":"<pre><code>/arena rotation &lt;time_between_resets&gt; &lt;arena1&gt; &lt;arena2&gt; [arena3...]\n</code></pre> <p>This configures all listed arenas as one rotation.</p> <p>Example:</p> <pre><code>/arena rotation 5m red blue green\n</code></pre> <p>This rotates the shared gameplay area between <code>red</code>, <code>blue</code>, and <code>green</code>, with one variant applying every 5 minutes.</p>"},{"location":"commands/#pause-or-resume-automatic-resets","title":"Pause Or Resume Automatic Resets","text":"<p>Pause all automatic periodic resets:</p> <pre><code>/arena pause &lt;time&gt;\n</code></pre> <p>Resume:</p> <pre><code>/arena pause off\n</code></pre> <p><code>resume</code> is also accepted:</p> <pre><code>/arena pause resume\n</code></pre> <p>Check status:</p> <pre><code>/arena pause status\n</code></pre> <p><code>/arena pause 0</code> also resumes automatic resets.</p>"},{"location":"commands/#reload-configuration","title":"Reload Configuration","text":"<pre><code>/arena reload\n</code></pre> <p>Reloads <code>config.yml</code>, display names, and configured periodic schedules.</p>"},{"location":"commands/#delete-arena","title":"Delete Arena","text":"<pre><code>/arena delete &lt;name&gt;\n</code></pre> <p>Deletes the arena from memory and removes its <code>.aarena</code> file if it exists.</p>"},{"location":"commands/#migration","title":"Migration","text":"<p>Use the default available provider:</p> <pre><code>/arena migrate &lt;name|all&gt;\n</code></pre> <p>Use a specific provider:</p> <pre><code>/arena migrate &lt;provider&gt; &lt;name|all&gt;\n</code></pre> <p>Providers:</p> <ul> <li><code>asyncarenas</code></li> <li><code>arearesetterpro</code></li> <li><code>areareloader-fawe</code></li> </ul> <p>See Migration Guide for behavior and naming rules.</p>"},{"location":"configuration/","title":"Configuration","text":"<p>The main config file is:</p> <pre><code>plugins/AsyncArenasPlus/config.yml\n</code></pre> <p>Run this after editing it:</p> <pre><code>/arena reload\n</code></pre>"},{"location":"configuration/#loader-license","title":"Loader License","text":"<p>If you bought AsyncArenasPlus, start the server once after installing the plugin jar. The plugin creates:</p> <pre><code>plugins/AsyncArenasPlus/loader.yml\n</code></pre> <p>Put the license from the purchase Discord into <code>loader.yml</code>, then restart the server.</p>"},{"location":"configuration/#periodic-resets","title":"Periodic Resets","text":"<pre><code>periodic-resets: {}\nperiodic-offsets: {}\n</code></pre> <code>periodic-resets</code> Controls how often each arena resets automatically. The key is the arena name and the value is the reset interval. <code>periodic-offsets</code> Optional. Delays when an arena starts its reset schedule. This is useful when you have multiple arenas and do not want all of them resetting at the same time. <p>Example:</p> <pre><code>periodic-resets:\n  mine_1: 15m\n  mine_2: 15m\nperiodic-offsets:\n  mine_2: 5m\n</code></pre> <p>In this example, both arenas reset every 15 minutes, but <code>mine_2</code> starts 5 minutes later than <code>mine_1</code>.</p> <p>Most arenas do not need an offset.</p> <p>Time values support <code>10s</code>, <code>5m</code>, <code>3h</code>, <code>1d</code>, <code>20t</code>, or a raw tick value with no suffix. Use explicit units in normal configs.</p>"},{"location":"configuration/#display-names","title":"Display Names","text":"<pre><code>arena-display-names: {}\n</code></pre> <p>Arena files use the name from <code>/arena create &lt;name&gt;</code>. Display names let command output and placeholders show a cleaner public name without renaming the arena file.</p> <p>Example:</p> <pre><code>arena-display-names:\n  mine_1: Mine Arena 1\n</code></pre>"},{"location":"configuration/#reset-packets","title":"Reset Packets","text":"<pre><code>reset-packets:\n  use-precise-dirty-blocks: false\n  send-light-updates: true\n</code></pre> <code>use-precise-dirty-blocks</code> Advanced/debug option for reset packet behavior. Do not change this unless you know what you are doing or have been asked to while troubleshooting. <code>send-light-updates</code> Sends lighting updates to players after arena resets. Keep this enabled if players need lighting to update immediately after a reset."},{"location":"configuration/#reset-section-preparation","title":"Reset Section Preparation","text":"<pre><code>reset-sections:\n  preparation-mode: construct-from-scratch\n</code></pre> <p>Supported values:</p> <ul> <li><code>construct-from-scratch</code>: safest option and the recommended default.</li> <li><code>copy</code>: can be faster, but can cause weird issues in certain server/plugin contexts.</li> </ul> <p>Stick to <code>construct-from-scratch</code> unless you have tested your server, confirmed this part is actually too slow, and verified that <code>copy</code> behaves correctly in your setup.</p>"},{"location":"configuration/#update-queue","title":"Update Queue","text":"<pre><code>update-queue:\n  enabled: true\n  max-sections-per-player-per-tick: 6\n  max-queued-sections-per-player: 10000\n</code></pre> <code>enabled</code> Controls whether visible reset updates are smoothed out for players instead of being sent all at once. <code>max-sections-per-player-per-tick</code> Controls how quickly each player receives visible reset updates. Higher values make reset visuals appear faster, but send more packets at once. <code>max-queued-sections-per-player</code> Limits how many visible reset updates can wait for one player. This prevents unbounded queues during very large resets."},{"location":"configuration/#arena-files","title":"Arena Files","text":"<p>AsyncArenasPlus stores arena files in:</p> <pre><code>plugins/AsyncArenasPlus/arenas/\n</code></pre> <p>Each arena is stored as:</p> <pre><code>&lt;arenaName&gt;.aarena\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#1-install-plugins","title":"1. Install plugins","text":"<p>Required:</p> <ul> <li>AsyncArenasPlus</li> </ul> <p>No other plugin is required for normal arena creation and resets.</p>"},{"location":"getting-started/#2-license-loader-setup","title":"2. License loader setup","text":"<p>If you bought AsyncArenasPlus:</p> <ol> <li>Put the plugin jar in your server's <code>plugins</code> folder.</li> <li>Start the server once.</li> <li>Stop the server.</li> <li>Open:</li> </ol> <pre><code>plugins/AsyncArenasPlus/loader.yml\n</code></pre> <ol> <li>Add the license from the purchase Discord.</li> <li>Start the server again.</li> </ol> <p>Optional:</p> <ul> <li>PlaceholderAPI, if you want reset countdown placeholders.</li> <li>AsyncArenas, AreaResetterPro, or AreaReloader-FAWE, if you want to migrate existing arenas.</li> </ul>"},{"location":"getting-started/#3-create-your-first-arena","title":"3. Create your first arena","text":"<ol> <li>Get the arena selection item:</li> </ol> <pre><code>/arena wand\n</code></pre> <ol> <li> <p>Use the amethyst shard to select two corners of your arena area.</p> </li> <li> <p>Create the arena:</p> </li> </ol> <pre><code>/arena create &lt;name&gt;\n</code></pre> <p>Optional: create and set periodic reset in one step:</p> <pre><code>/arena create &lt;name&gt; &lt;time&gt;\n</code></pre> <p>Tip: you can also create by coordinates:</p> <pre><code>/arena create &lt;name&gt; &lt;world&gt; &lt;x1&gt; &lt;y1&gt; &lt;z1&gt; &lt;x2&gt; &lt;y2&gt; &lt;z2&gt; [time]\n</code></pre> <p>Time values support <code>10s</code>, <code>5m</code>, <code>3h</code>, <code>1d</code>, <code>20t</code>, or a raw tick value with no suffix.</p>"},{"location":"getting-started/#4-verify-reset","title":"4. Verify reset","text":"<ol> <li>Change blocks inside the arena.</li> <li>Run:</li> </ol> <pre><code>/arena reset &lt;name&gt;\n</code></pre>"},{"location":"getting-started/#5-set-periodic-resets","title":"5. Set periodic resets","text":"<p>Basic:</p> <pre><code>/arena periodic &lt;name&gt; &lt;time&gt;\n</code></pre> <p>Offset is optional. Most arenas do not need one.</p> <p>Optional offset example:</p> <pre><code>/arena periodic &lt;name&gt; &lt;time&gt; &lt;offset&gt;\n</code></pre> <p>Example:</p> <pre><code>/arena periodic pvp_1 15m 2m\n</code></pre> <p>This means:</p> <ul> <li><code>pvp_1</code> resets every 15 minutes</li> <li>it waits 2 minutes before starting that schedule, which can help spread resets out</li> </ul> <p>Use <code>/arena pause &lt;time&gt;</code> to temporarily stop all automatic periodic resets, <code>/arena pause off</code> to resume, and <code>/arena pause status</code> to check the current state.</p>"},{"location":"getting-started/#6-rotate-one-gameplay-area-between-multiple-terrain-variants","title":"6. Rotate one gameplay area between multiple terrain variants","text":"<p>If you want one physical area to cycle through different terrain versions:</p> <ol> <li>Create arena variant A for that area.</li> <li>Change the area to variant B and create a second arena with the same bounds.</li> <li>Run the rotation command.</li> </ol> <p>Example:</p> <pre><code>/arena rotation 5m arena_variant_a arena_variant_b\n</code></pre> <p>This makes the same area alternate between stored variants every 5 minutes.</p>"},{"location":"getting-started/#7-inspect-the-result","title":"7. Inspect the result","text":"<p>Useful checks after setup:</p> <pre><code>/arena list\n/arena info &lt;name&gt;\n</code></pre> <p><code>/arena info &lt;name&gt;</code> shows bounds, display name, and next periodic reset timing.</p>"},{"location":"main-features/","title":"Main Features","text":""},{"location":"main-features/#arena-creation","title":"Arena creation","text":"<p>Use <code>/arena wand</code>, click two corners, then run <code>/arena create &lt;name&gt;</code>.</p> <p>This is the normal workflow for creating new arenas.</p> <p>Console users, command blocks, and scripted setup can create arenas with exact coordinates:</p> <pre><code>/arena create &lt;name&gt; &lt;world&gt; &lt;x1&gt; &lt;y1&gt; &lt;z1&gt; &lt;x2&gt; &lt;y2&gt; &lt;z2&gt; [time]\n</code></pre>"},{"location":"main-features/#manual-arena-resets","title":"Manual arena resets","text":"<p>Use:</p> <pre><code>/arena reset &lt;name&gt;\n</code></pre> <p>for immediate resets during events, testing, or moderation.</p>"},{"location":"main-features/#editing-saved-templates","title":"Editing saved templates","text":"<p>To update the saved version of an arena:</p> <ol> <li>Pause automatic resets.</li> <li>Reset the arena you want to modify.</li> <li>Make your changes in the world.</li> <li>Save the new version.</li> </ol> <p>Commands:</p> <pre><code>/arena pause &lt;time&gt;\n/arena reset &lt;name&gt;\n/arena edit &lt;name&gt;\n</code></pre> <p>After you are done, resume automatic resets:</p> <pre><code>/arena pause off\n</code></pre>"},{"location":"main-features/#periodic-resets","title":"Periodic resets","text":"<p>Use:</p> <pre><code>/arena periodic &lt;name&gt; &lt;time&gt; [offset]\n</code></pre> <p>How it behaves:</p> <ul> <li><code>time</code>: how often the arena resets</li> <li><code>offset</code>: optional delay before that arena starts its schedule</li> <li>You do not need to specify an offset.</li> </ul> <p>You can globally pause/resume automatic periodic resets with:</p> <pre><code>/arena pause &lt;time|off|status&gt;\n</code></pre> <p>Time values support <code>10s</code>, <code>5m</code>, <code>3h</code>, <code>1d</code>, <code>20t</code>, or a raw tick value with no suffix.</p>"},{"location":"main-features/#terrain-rotation","title":"Terrain rotation","text":"<p>Use rotation when one gameplay area should cycle through multiple terrain variants.</p> <p>Typical setup:</p> <ol> <li>Create multiple arenas that cover the same area, each saved in a different terrain state.</li> <li>Run the rotation command.</li> </ol> <pre><code>/arena rotation &lt;time_between_resets&gt; &lt;arena1&gt; &lt;arena2&gt; [arena3...]\n</code></pre> <p>Example:</p> <pre><code>/arena rotation 5m red blue green\n</code></pre> <p>This rotates the shared gameplay area between <code>red</code>, <code>blue</code>, and <code>green</code>, with one variant applying every 5 minutes.</p>"},{"location":"main-features/#display-names","title":"Display names","text":"<p>Arena files use the name you entered when creating the arena. If that name is not clean enough for public placeholders or command output, set a display name:</p> <pre><code>/arena displayname &lt;arena&gt; &lt;new display name&gt;\n</code></pre> <p>Display names are shown by <code>/arena list</code>, <code>/arena info</code>, and PlaceholderAPI placeholders that return arena names.</p>"},{"location":"main-features/#placeholderapi-reset-countdowns","title":"PlaceholderAPI reset countdowns","text":"<p>When PlaceholderAPI is installed, AsyncArenasPlus registers the <code>asyncarenasplus</code> expansion.</p> <p>Supported placeholders:</p> <ul> <li><code>%asyncarenasplus_reset_time_&lt;arena&gt;%</code></li> <li><code>%asyncarenasplus_reset_soonest_time_&lt;arena1,arena2,...&gt;%</code></li> <li><code>%asyncarenasplus_reset_soonest_name_&lt;arena1,arena2,...&gt;%</code></li> </ul> <p>See PlaceholderAPI for examples and return values.</p>"},{"location":"main-features/#low-lag-resets-and-compatibility","title":"Low-lag resets and compatibility","text":"<p>AsyncArenasPlus is built to reset arenas without lag during normal gameplay.</p> <p>An optional WorldEdit compatibility layer is available for servers that use external editing workflows.</p>"},{"location":"main-features/#migration-from-other-arena-plugins","title":"Migration from other arena plugins","text":"<p>You can import existing arenas from supported providers using <code>/arena migrate ...</code>.</p> <p>Current providers:</p> <ul> <li><code>asyncarenas</code></li> <li><code>arearesetterpro</code></li> <li><code>areareloader-fawe</code></li> </ul>"},{"location":"migrations/","title":"Migration Guide","text":"<p>AsyncArenasPlus can import arenas from supported plugins.</p>"},{"location":"migrations/#supported-sources","title":"Supported Sources","text":"<ul> <li><code>asyncarenas</code></li> <li><code>arearesetterpro</code></li> <li><code>areareloader-fawe</code></li> </ul>"},{"location":"migrations/#requirement","title":"Requirement","text":"<p>The plugin you are migrating from must be installed, enabled, and running on the server while you migrate.</p> <p>After the migration is complete and you have confirmed the arenas work in AsyncArenasPlus, you can disable or remove the old plugin.</p>"},{"location":"migrations/#commands","title":"Commands","text":"<p>Migrate using the first available source plugin:</p> <pre><code>/arena migrate &lt;name|all&gt;\n</code></pre> <p>Migrate from a specific source plugin:</p> <pre><code>/arena migrate &lt;provider&gt; &lt;name|all&gt;\n</code></pre> <p>Examples:</p> <pre><code>/arena migrate all\n/arena migrate asyncarenas all\n/arena migrate arearesetterpro mine_1\n</code></pre>"},{"location":"migrations/#after-migration","title":"After Migration","text":"<p>Check the imported arenas:</p> <pre><code>/arena list\n/arena info &lt;arena&gt;\n/arena reset &lt;arena&gt;\n</code></pre> <p>If you want a nicer public name, set one with:</p> <pre><code>/arena displayname &lt;arena&gt; &lt;new display name&gt;\n</code></pre>"},{"location":"minecraft-region-reset-plugin/","title":"Minecraft Region Reset Plugins: AsyncArenasPlus vs WorldEdit, FAWE, and Arena Reset Tools","text":"<p>If you are searching for a Minecraft Paper plugin to reset a region automatically, regenerate an area on a schedule, or reset a spawn area without lag, there are several valid options.</p> <p>For basic periodic arena or area resetting, you do not need AsyncArenasPlus. If your arena is small, player count is low, or occasional reset lag is acceptable, use a simpler free plugin.</p> <p>This page explains when common reset tools work well, where they start to struggle, and why AsyncArenasPlus exists.</p>"},{"location":"minecraft-region-reset-plugin/#common-search-queries-this-page-answers","title":"Common Search Queries This Page Answers","text":"<ul> <li>Minecraft Paper plugin reset region automatically WorldEdit WorldGuard regen area scheduler</li> <li>Minecraft server automatically reset spawn area plugin Paper Spigot</li> <li>WorldEdit regenerate command region Minecraft</li> <li>Minecraft plugin scheduled area reset</li> <li>Minecraft arena reset plugin without lag</li> </ul>"},{"location":"minecraft-region-reset-plugin/#basic-resetting-options","title":"Basic Resetting Options","text":"<p>For simple scheduled region resets, these are good options to look at:</p> <ul> <li>AsyncArenas, a paid plugin for basic arena reset workflows.</li> <li>AreaReset on BuiltByBit, a free and open source area reset plugin.</li> <li>Blossom, a free and open source arena regeneration plugin.</li> <li>FastAsyncWorldEdit or WorldEdit commands, free options if you are comfortable building your own workflow around schematics and region commands.</li> </ul> <p>For basic periodic arena resets, the free alternatives usually cover roughly the same feature set and have similar practical performance characteristics to base AsyncArenas. If all you need is a simple scheduled area reset, use the free alternatives first. You usually do not need to pay for base AsyncArenas for that use case.</p> <p>These tools can be a reasonable choice when:</p> <ul> <li>The reset area is small.</li> <li>Few players can see the area during resets.</li> <li>You are resetting during low-traffic moments.</li> <li>You only need a simple timer or manual command.</li> <li>You are fine with a FAWE or WorldEdit-based reset workflow.</li> </ul>"},{"location":"minecraft-region-reset-plugin/#why-regen-or-schematic-pasting-is-not-always-enough","title":"Why <code>/regen</code> Or Schematic Pasting Is Not Always Enough","text":"<p>WorldEdit-style regeneration and schematic pasting solve the block-changing part of the problem. They can restore terrain, reload a schematic, or regenerate a selected area.</p> <p>That is not the full performance problem on a live Paper or Spigot server.</p> <p>When a large visible area changes, every nearby player who can see that area needs to receive client updates. With small arenas and low player counts, this is usually fine. With bigger arenas and many nearby players, the network side becomes the bottleneck.</p> <p>On servers with roughly 50 or more players near the reset area, a large reset can send so many packets at once that players briefly freeze or see the world update in a harsh burst. This gets worse as:</p> <ul> <li>The arena gets larger.</li> <li>More players can see the arena.</li> <li>The reset changes more visible blocks.</li> <li>Multiple reset areas run close together.</li> </ul> <p>This is the issue AsyncArenasPlus is designed to solve. The focus is not \"basic scheduled reset\"; the focus is solving the live-player packet/update problem that larger servers run into.</p>"},{"location":"minecraft-region-reset-plugin/#the-fawe-based-reset-bottleneck","title":"The FAWE-Based Reset Bottleneck","text":"<p>Many region reset solutions are built around FAWE, WorldEdit, or schematic paste workflows. That is not automatically bad. FAWE is excellent software and is often the right tool for editing worlds.</p> <p>The limitation is that FAWE-based reset workflows usually focus on applying the world change quickly. For a busy public server, the harder problem is often what happens after the world change: player-visible updates need to be sent to many clients.</p> <p>FAWE is not really built around repeated live arena resets where the same area is pasted back again and again while many players are watching. It is built as a powerful world editing tool. In reset workflows, it generally applies the requested write/paste operation and lets the resulting updates happen, rather than treating the reset as a specialized problem where every changed block and every packet should be checked for whether it is actually required or optimal.</p> <p>If every visible update is pushed at once, the server can be fine internally while players still experience a short network freeze.</p>"},{"location":"minecraft-region-reset-plugin/#what-asyncarenasplus-does-differently","title":"What AsyncArenasPlus Does Differently","text":"<p>AsyncArenasPlus is also a paid plugin, but it is built for a different problem than basic area reset tools. It does not depend on FAWE for resets.</p> <p>If a free reset plugin works for your server, use it. AsyncArenasPlus is for the cases where those plugins hit the visible reset bottleneck: larger areas, many nearby players, and resets that need to happen while the server is active.</p> <p>Instead, it is built around arena snapshots, low-lag reset execution, and smarter player update delivery. At a high level, it tries to:</p> <ul> <li>Avoid sending client updates when they are not needed.</li> <li>Send as little reset data as possible.</li> <li>Spread visible updates over time instead of dumping everything at once.</li> <li>Prioritize updates that matter first, so nearby or important changes appear earlier.</li> <li>Support scheduled resets, manual resets, and terrain rotations from one workflow.</li> </ul> <p>The goal is simple: reset arenas or server areas while players are online without the visible reset causing a packet spike.</p>"},{"location":"minecraft-region-reset-plugin/#which-plugin-should-you-use","title":"Which Plugin Should You Use?","text":"Use case Good fit Small arena, few players nearby, simple timer Free options like AreaReset, Blossom, or a FAWE-based setup Basic paid arena reset workflow Usually unnecessary; try the free alternatives first Admin-only schematic reset workflow WorldEdit or FastAsyncWorldEdit Spawn area or PvP arena that resets while many players are nearby AsyncArenasPlus Large arena rotations with players watching AsyncArenasPlus Migrating from an older arena reset plugin AsyncArenasPlus migration tools"},{"location":"minecraft-region-reset-plugin/#example-scenarios","title":"Example Scenarios","text":""},{"location":"minecraft-region-reset-plugin/#small-practice-arena","title":"Small practice arena","text":"<p>If 5 to 10 players use a small practice area, a simple periodic reset plugin may be enough.</p>"},{"location":"minecraft-region-reset-plugin/#public-spawn-mine-or-event-area","title":"Public spawn mine or event area","text":"<p>If 50 or more players can see the area when it resets, the packet spike matters. This is where AsyncArenasPlus is built to be different.</p>"},{"location":"minecraft-region-reset-plugin/#rotating-pvp-maps","title":"Rotating PvP maps","text":"<p>If one physical arena should rotate between multiple saved terrain variants, AsyncArenasPlus can schedule that rotation directly:</p> <pre><code>/arena rotation 5m arena_variant_a arena_variant_b arena_variant_c\n</code></pre>"},{"location":"minecraft-region-reset-plugin/#faq","title":"FAQ","text":""},{"location":"minecraft-region-reset-plugin/#can-i-just-use-worldedit-regenerate","title":"Can I just use WorldEdit regenerate?","text":"<p>For manual admin work, yes. For automatic live resets on a busy server, it is usually not enough by itself. The player-visible update burst is the part that becomes painful.</p>"},{"location":"minecraft-region-reset-plugin/#is-fawe-bad-for-arena-resets","title":"Is FAWE bad for arena resets?","text":"<p>No. FAWE is good at world editing. The problem is not that FAWE is bad; the problem is that large visible resets have a network-update cost that basic paste/regeneration workflows do not fully solve.</p>"},{"location":"minecraft-region-reset-plugin/#is-asyncarenasplus-only-for-minigame-arenas","title":"Is AsyncArenasPlus only for minigame arenas?","text":"<p>No. You can use it for arenas, mines, event areas, spawn areas, or any server area that should return to a saved state.</p>"},{"location":"minecraft-region-reset-plugin/#do-i-need-asyncarenasplus-for-every-server","title":"Do I need AsyncArenasPlus for every server?","text":"<p>No. If your reset areas are small or rarely watched by many players, use a simpler free or open source reset plugin.</p> <p>AsyncArenasPlus is for servers where reset performance and player-visible smoothness matter.</p>"},{"location":"placeholders/","title":"PlaceholderAPI","text":"<p>AsyncArenasPlus registers PlaceholderAPI placeholders only when PlaceholderAPI is installed and enabled.</p> <p>Identifier:</p> <pre><code>asyncarenasplus\n</code></pre>"},{"location":"placeholders/#single-arena-reset-time","title":"Single Arena Reset Time","text":"<pre><code>%asyncarenasplus_reset_time_&lt;arena&gt;%\n</code></pre> <p>Returns the time until the arena's next periodic reset.</p> <p>Examples:</p> <pre><code>%asyncarenasplus_reset_time_mine_1%\n%asyncarenasplus_reset_time_pvp_1%\n</code></pre> <p>Return values:</p> <ul> <li><code>now</code>: reset time is due or overdue</li> <li><code>never</code>: arena does not exist or has no next periodic reset scheduled</li> <li>a compact duration such as <code>45s</code>, <code>5m</code>, <code>1h 20m</code>, or <code>2d 3h 10m</code></li> </ul>"},{"location":"placeholders/#soonest-reset-time","title":"Soonest Reset Time","text":"<pre><code>%asyncarenasplus_reset_soonest_time_&lt;arena1,arena2,...&gt;%\n</code></pre> <p>Returns the shortest reset countdown from a comma-separated arena list.</p> <p>Example:</p> <pre><code>%asyncarenasplus_reset_soonest_time_mine_1,mine_2,mine_3%\n</code></pre> <p>Return values:</p> <ul> <li><code>never</code>: no listed arena has a scheduled next reset</li> <li>a compact duration such as <code>20s</code>, <code>4m</code>, or <code>1h 5m</code></li> </ul>"},{"location":"placeholders/#soonest-reset-name","title":"Soonest Reset Name","text":"<pre><code>%asyncarenasplus_reset_soonest_name_&lt;arena1,arena2,...&gt;%\n</code></pre> <p>Returns the display name of the listed arena with the soonest scheduled reset.</p> <p>Example:</p> <pre><code>%asyncarenasplus_reset_soonest_name_mine_1,mine_2,mine_3%\n</code></pre> <p>Return values:</p> <ul> <li><code>none</code>: no listed arena has a scheduled next reset</li> <li>the arena display name, if configured</li> <li>otherwise the arena's internal name</li> </ul>"},{"location":"placeholders/#notes","title":"Notes","text":"<ul> <li>Arena names are comma-separated with no special escaping.</li> <li>Blank arena names in a list are ignored.</li> <li>Display names are configured with <code>/arena displayname &lt;arena&gt; &lt;new display name&gt;</code>.</li> <li>Placeholders use scheduled periodic reset times; manual resets do not create a future scheduled reset by themselves.</li> </ul>"},{"location":"troubleshooting/","title":"Troubleshooting","text":""},{"location":"troubleshooting/#arena-is-not-resetting-as-expected","title":"Arena is not resetting as expected","text":"<ol> <li>Confirm the arena exists:</li> </ol> <pre><code>/arena list\n</code></pre> <ol> <li>Check the arena details:</li> </ol> <pre><code>/arena info &lt;name&gt;\n</code></pre> <ol> <li>Run a manual reset:</li> </ol> <pre><code>/arena reset &lt;name&gt;\n</code></pre> <p>If the saved arena version is wrong, update it with the safe edit workflow:</p> <pre><code>/arena pause &lt;time&gt;\n/arena reset &lt;name&gt;\n</code></pre> <p>Make your changes, then save them:</p> <pre><code>/arena edit &lt;name&gt;\n/arena pause off\n</code></pre>"},{"location":"troubleshooting/#players-see-resets-slowly","title":"Players see resets slowly","text":"<p>Large arenas may need more time to visually update for every player.</p> <p>If you know what you are doing, you can adjust:</p> <pre><code>update-queue:\n  enabled: true\n  max-sections-per-player-per-tick: 6\n</code></pre> <p>Higher values make reset visuals appear faster but can increase packet bursts.</p>"},{"location":"troubleshooting/#light-looks-wrong-after-a-reset","title":"Light looks wrong after a reset","text":"<p>Make sure light update packets are enabled:</p> <pre><code>reset-packets:\n  send-light-updates: true\n</code></pre> <p>If the arena was saved with bad lighting, fix the lighting in-world and run:</p> <pre><code>/arena edit &lt;name&gt;\n</code></pre>"},{"location":"troubleshooting/#migration-provider-says-not-available","title":"Migration provider says \"not available\"","text":"<p>The plugin you are migrating from must be:</p> <ul> <li>installed</li> <li>enabled</li> <li>running on the same server while you migrate</li> </ul> <p>After migration is complete, you can disable or remove the old plugin.</p>"},{"location":"troubleshooting/#migration-fails-for-one-arena","title":"Migration fails for one arena","text":"<p>Try migrating one arena at a time:</p> <pre><code>/arena migrate &lt;provider&gt; &lt;arena&gt;\n</code></pre> <p>Confirm that the old plugin can still load or reset that arena before migrating it.</p>"},{"location":"troubleshooting/#periodic-resets-feel-bursty","title":"Periodic resets feel bursty","text":"<p>Use the rotation helper for shared-area rotations:</p> <pre><code>/arena rotation &lt;time_between_resets&gt; &lt;arena1&gt; &lt;arena2&gt; [arena3...]\n</code></pre> <p>For unrelated arenas, offsets are optional. Use one only when you want an arena to start its schedule later than another arena:</p> <pre><code>/arena periodic &lt;name&gt; &lt;time&gt; &lt;offset&gt;\n</code></pre>"},{"location":"troubleshooting/#automatic-resets-need-to-stop-temporarily","title":"Automatic resets need to stop temporarily","text":"<p>Pause every automatic periodic reset:</p> <pre><code>/arena pause 5m\n</code></pre> <p>Resume:</p> <pre><code>/arena pause off\n</code></pre> <p>Check status:</p> <pre><code>/arena pause status\n</code></pre> <p>Manual <code>/arena reset &lt;name&gt;</code> still works while automatic resets are paused.</p>"}]}